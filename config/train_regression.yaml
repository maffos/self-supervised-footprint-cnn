tag: "regression"
model_name: 'FeatureExtractorCNN'
data:
  transform: [swap_channels]
  bucketize: False
  batch_size: 1
device: 'cuda'
loss_fn: 'ScaledMAE'
training:
  lr: 1.e-4
  lr_min: 1.e-6
  num_epochs: 1
  weight_decay: 1.0e-6
model:
  hidden_dims: [256,256,256,256]
  norm: null
  activation: relu
  conv_type: standard
  mlp_norm: null
  swap_channels: True

